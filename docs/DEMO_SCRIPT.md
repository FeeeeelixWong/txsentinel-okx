# 90-Second Demo Script

Use the final recorded voice track as the timing source, then generate subtitles from that audio. Do not time subtitles from this draft alone.

| Time | Screen action | Voiceover |
| --- | --- | --- |
| 0:00-0:08 | Product name and first screen | "Autonomous agents can execute transactions, but executable does not mean authorized. This is TxSentinel, a policy firewall that checks the action before the agent signs." |
| 0:08-0:20 | Show the three scenarios and action form | "An agent submits its proposed chain action, the user's guardrails, and simulation evidence. TxSentinel never receives a private key and cannot sign or broadcast." |
| 0:20-0:32 | Run Routine transfer | "This X Layer transfer is below the spend and fee caps, and its simulation succeeded. The live API returns ALLOW, risk zero, and a deterministic receipt." |
| 0:32-0:45 | Run Spend cap breach | "Now the same workflow proposes an eight-hundred-fifty-dollar swap against a five-hundred-dollar mandate. Execution may be possible, but policy returns HOLD with exact limit evidence." |
| 0:45-0:58 | Run Unlimited approval | "An unlimited token approval crosses a hard boundary. TxSentinel returns DENY, risk one hundred, and identifies the violated rule without exposing signing authority." |
| 0:58-1:10 | Open raw response and point to hashes | "Every response contains the normalized action, policy snapshot, evidence, risk score, action digest, and SHA-256 receipt. Equivalent inputs reproduce the same receipt hash." |
| 1:10-1:22 | Show API/x402 status and architecture | "The free endpoint is live for ASP review. A separate paid endpoint uses the official OKX x402 Express, Core, and EVM SDKs so an Agentic Wallet can pay per policy check on X Layer." |
| 1:22-1:30 | Return to product and GitHub link | "TxSentinel separates what an agent can do from what it is allowed to do: one pre-sign decision boundary for safer autonomous finance." |

## Recording Checks

- Keep the video under 90 seconds.
- Record at 1440 by 900 or 1920 by 1080.
- Keep browser zoom at 100 percent.
- Record the three API calls live.
- Show the receipt hash changing between scenarios.
- Burn subtitles from the final narration timestamps.
